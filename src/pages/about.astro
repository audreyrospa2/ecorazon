---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
---

<Layout title="About Us - Ecorazón">
  <Header />
  
  <main>
    <!-- Hero Section -->
    <section class="bg-gradient-to-br from-warm-beige to-gray-100 py-16 hero-about">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
        <h1 class="text-4xl md:text-5xl font-bold text-forest-green mb-6 animate-fade-in">
          About Ecorazón
        </h1>
        <p class="text-xl text-gray-700 max-w-3xl mx-auto animate-fade-in-delay">
          Where nature's echo meets the art with heart
        </p>
      </div>
    </section>

    <!-- Who We Are Section -->
    <section class="py-16 bg-white">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
          <div class="who-we-are-text">
            <h2 class="text-3xl md:text-4xl font-bold text-forest-green mb-6">
              Who We Are
            </h2>
            <p class="text-lg text-gray-700 mb-6 leading-relaxed">
              Ecorazón partners with talented artists with special needs to create premium, custom-designed products for businesses. We specialize in sustainable corporate gifting that delivers social impact alongside quality craftsmanship.
            </p>
            
            <div class="space-y-4">
              <div class="flex items-start feature-item">
                <div class="text-2xl mr-4">✨</div>
                <div>
                  <h3 class="text-xl font-semibold text-forest-green mb-1">Social Impact Through Art</h3>
                  <p class="text-gray-700">Support talented artists with special needs while enhancing your corporate gifting program. Every purchase creates meaningful employment opportunities.</p>
                </div>
              </div>
              
              <div class="flex items-start feature-item">
                <div class="text-2xl mr-4">🌱</div>
                <div>
                  <h3 class="text-xl font-semibold text-forest-green mb-1">Sustainable & Eco-Friendly</h3>
                  <p class="text-gray-700">All products use environmentally responsible materials and production methods, helping your brand reduce its environmental footprint.</p>
                </div>
              </div>
              
              <div class="flex items-start feature-item">
                <div class="text-2xl mr-4">⏰</div>
                <div>
                  <h3 class="text-xl font-semibold text-forest-green mb-1">Quality That Lasts</h3>
                  <p class="text-gray-700">Durable, thoughtfully designed gifts that recipients will value and remember, ensuring your brand makes a lasting impression.</p>
                </div>
              </div>
            </div>
          </div>
          
          <div class="bg-warm-beige p-8 rounded-lg who-we-are-image">
            <img 
                src="/images/green.jpg" 
                alt="Ecorazón showcase" 
                class="w-full h-64 object-cover rounded-lg"
            />
          </div>
        </div>
      </div>
    </section>

    <!-- Our Story Section -->
    <section class="py-16 bg-warm-beige">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-12">
          <h2 class="text-3xl md:text-4xl font-bold text-forest-green mb-6 story-title">
            Our Story
          </h2>
        </div>
        
        <div class="max-w-4xl mx-auto">
          <div class="bg-white p-8 rounded-lg shadow-sm story-content">
            <p class="text-lg text-gray-700 mb-6 leading-relaxed">
              Ecorazón was born from a simple belief: every individual is a masterpiece of nature.
            </p>
            <p class="text-lg text-gray-700 leading-relaxed">
              The name Ecorazón combines "Eco," representing ecology and sustainability, with the Spanish word "Corazón" (heart). It also evokes the idea of an echo — the resonance of nature's voice within the human spirit.
            </p>
          </div>
        </div>
      </div>
    </section>

    <!-- Mission & Vision Section -->
    <section class="py-16 bg-white">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-12">
          <div class="bg-forest-green text-white p-8 rounded-lg mission-card">
            <h2 class="text-3xl font-bold mb-6">Our Mission</h2>
            <p class="text-lg leading-relaxed">
              To listen deeply to the voice of nature, capture its inspiration, and transform these echoes into heartfelt, sustainable creations.
            </p>
          </div>
          
          <div class="bg-deep-olive text-white p-8 rounded-lg vision-card">
            <h2 class="text-3xl font-bold mb-6">Our Vision</h2>
            <p class="text-lg leading-relaxed">
              To inspire more people to see themselves as miracles of nature and cherish the purest beauty of this planet we call home.
            </p>
          </div>
        </div>
      </div>
    </section>
  </main>
  
  <Footer />
</Layout>

<style>
  /* Hero animations - load immediately */
  @keyframes fadeInDown {
    from {
      opacity: 0;
      transform: translateY(-20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .animate-fade-in {
    animation: fadeInDown 0.8s ease-out 0.2s both;
  }

  .animate-fade-in-delay {
    animation: fadeInDown 0.8s ease-out 0.4s both;
  }

  /* Scroll-triggered animations */
  .who-we-are-text,
  .who-we-are-image,
  .feature-item,
  .story-title,
  .story-content,
  .mission-card,
  .vision-card {
    opacity: 0;
    transform: translateY(30px);
    transition: opacity 0.8s ease-out, transform 0.8s ease-out;
  }

  .who-we-are-text.visible {
    opacity: 1;
    transform: translateY(0);
    transition-delay: 0.1s;
  }

  .who-we-are-image.visible {
    opacity: 1;
    transform: translateY(0);
    transition-delay: 0.3s;
  }

  .feature-item.visible {
    opacity: 1;
    transform: translateY(0);
  }

  .feature-item:nth-child(1).visible {
    transition-delay: 0.2s;
  }

  .feature-item:nth-child(2).visible {
    transition-delay: 0.4s;
  }

  .feature-item:nth-child(3).visible {
    transition-delay: 0.6s;
  }

  .story-title.visible {
    opacity: 1;
    transform: translateY(0);
    transition-delay: 0.1s;
  }

  .story-content.visible {
    opacity: 1;
    transform: translateY(0);
    transition-delay: 0.3s;
  }

  .mission-card.visible {
    opacity: 1;
    transform: translateY(0);
    transition-delay: 0.1s;
  }

  .vision-card.visible {
    opacity: 1;
    transform: translateY(0);
    transition-delay: 0.3s;
  }

  /* Hover effects */
  .feature-item {
    transition: opacity 0.8s ease-out, transform 0.8s ease-out, background-color 0.3s ease;
    padding: 1rem;
    border-radius: 0.5rem;
    cursor: pointer;
  }

  .feature-item:hover {
    background-color: rgba(139, 186, 139, 0.1);
    transform: translateX(10px) translateY(0);
  }

  .who-we-are-image {
    transition: opacity 0.8s ease-out, transform 0.8s ease-out, box-shadow 0.3s ease;
  }

  .who-we-are-image:hover {
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.15);
  }

  .story-content {
    transition: opacity 0.8s ease-out, transform 0.8s ease-out, box-shadow 0.3s ease;
  }

  .story-content:hover {
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.15);
  }

  .mission-card,
  .vision-card {
    transition: opacity 0.8s ease-out, transform 0.8s ease-out, box-shadow 0.3s ease;
    cursor: pointer;
  }

  .mission-card:hover,
  .vision-card:hover {
    box-shadow: 0 15px 35px rgba(0, 0, 0, 0.2);
    transform: translateY(-5px);
  }

  /* Smooth scrolling */
  html {
    scroll-behavior: smooth;
  }
</style>

<script>
  // Intersection Observer for scroll animations
  const observerOptions = {
    threshold: 0.2,
    rootMargin: '0px 0px -50px 0px'
  };

  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('visible');
      }
    });
  }, observerOptions);

  // Observe all animated elements
  document.addEventListener('DOMContentLoaded', () => {
    const elementsToAnimate = document.querySelectorAll(
      '.who-we-are-text, .who-we-are-image, .feature-item, .story-title, .story-content, .mission-card, .vision-card'
    );
    
    elementsToAnimate.forEach(el => observer.observe(el));
  });
</script>