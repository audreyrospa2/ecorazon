---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import productsRaw from '../data/products.json';
import taxonomyRaw from '../data/product-taxonomy.json';

type Product = {
  id: number;
  category: string;
  name: string;
  desc: string;
  img: string;
  series: string[];
  giftLevels: string[];
  tags: string[];
};

type Taxonomy = {
  series: string[];
  giftLevels: string[];
  tags: string[];
};

const products = productsRaw as Product[];
const taxonomy = taxonomyRaw as Taxonomy;

const categories = [
  { id: 'all', label: 'All' },
  { id: 'mice', label: 'Curated MICE' },
  { id: 'gifts', label: 'Corporate Gifts' },
  { id: 'wellness', label: 'Wellness Initiatives' },
  { id: 'esg', label: 'ESG Solutions' },
];
---

<Layout title="Product - Ecorazón">
  <Header />

  <main class="product-page">

    <!-- Page Hero -->
    <section class="hero-gifts">
      <canvas id="giftCanvas" aria-hidden="true"></canvas>

      <div class="hero-content">
        <h1>
          <span class="line-big">Couldn't find the right gift</span>
          <span class="line-big">for that one special day?</span>
        </h1>
        <p class="line-small">
          Make it meaningful with<br />
          Ecorazón Atelier
        </p>

        <div class="hero-actions">
          <a href="#intro" class="btn btn-ghost">Who are we?</a>
          <a href="#catalog" class="btn btn-solid">Shop now!</a>
        </div>
      </div>
    </section>

    <!-- Intro / typed-word section -->
    <section class="intro-section" id="intro">
      <div class="intro-content">
        <h2 class="intro-title">Ecorazón Atelier</h2>
        <p class="intro-tagline">Meaningful gifts, thoughtfully curated.</p>
        <p class="intro-line">
          We offer bespoke corporate gifts for brands that care — about <span class="typed-wrap">
            <span id="typedWord" class="typed-word"></span><span class="typed-cursor">|</span>
          </span>
        </p>
      </div>
    </section>

    <!-- Cycle Showcase -->
 
    <section class="cycle-showcase">
      <div class="cycle-hero-row">
        <span class="bg-letter bg-letter-left" aria-hidden="true">G</span>
    
        <div class="cycle-circles">
          <div class="cycle-circle circle-message">
            <span class="circle-arrow">↗</span>
            <p>Gifts with meaning created through sustainable materials, distinctive design and artist collaboration.</p>
          </div>
    
          <div class="cycle-circle circle-photo circle-photo-1" style="background-image:url('/images/c.jpg');">
          </div>
    
          <div class="cycle-circle circle-photo circle-photo-2" style="background-image:url('/images/c1.jpg');">
            <a href="#catalog" class="cycle-learn-more">
              Learn More <span>↗</span>
            </a>
          </div>
    
          <h2 class="cycle-wordmark">C.Y.C.L.E.</h2>
        </div>
    
        <span class="bg-letter bg-letter-right" aria-hidden="true">d</span>
      </div>
    
      <div class="cycle-cards">
        <div class="cycle-card">
          <div class="cycle-card-text">
            <h3>Original Design</h3>
            <a href="#catalog" class="cycle-explore-btn">Explore</a>
          </div>
          <div class="cycle-card-photo" style="background-image:url('/images/c3.jpg');"></div>
          <div class="cycle-card-circle" style="background-image:url('/images/c3.jpg');" aria-hidden="true"></div>
          <span class="cycle-card-arrow">↗</span>
        </div>

        <div class="cycle-card">
          <div class="cycle-card-text">
            <h3>Cultural Artistry</h3>
            <a href="#catalog" class="cycle-explore-btn">Explore</a>
          </div>
          <div class="cycle-card-photo" style="background-image:url('/images/c3.jpg');"></div>
          <div class="cycle-card-circle" style="background-image:url('/images/c3.jpg');" aria-hidden="true"></div>
          <span class="cycle-card-arrow">↗</span>
        </div>

        <div class="cycle-card">
          <div class="cycle-card-text">
            <h3>Sustainable Materials</h3>
            <a href="#catalog" class="cycle-explore-btn">Explore</a>
          </div>
          <div class="cycle-card-photo" style="background-image:url('/images/c3.jpg');"></div>
          <div class="cycle-card-circle" style="background-image:url('/images/c3.jpg');" aria-hidden="true"></div>
          <span class="cycle-card-arrow">↗</span>
        </div>
      </div>
    </section>

    <!-- Testimonials / Trust Section -->
    <section class="trust-section">
      <div class="trust-collage">

        <div class="trust-col trust-col-tall">
          <div class="trust-img" style="background-image:url('/images/1.png');"></div>
          <div class="trust-img" style="background-image:url('/images/2.png');"></div>
        </div>

        <div class="trust-col trust-col-short">
          <div class="trust-img" style="background-image:url('/images/3.png');"></div>
          <div class="trust-img" style="background-image:url('/images/4.png');"></div>
          <div class="trust-img" style="background-image:url('/images/5.png');"></div>
        </div>

        <div class="trust-col trust-col-mid">
          <div class="trust-img trust-img-lg" style="background-image:url('/images/6.png');"></div>
          <div class="trust-img" style="background-image:url('/images/7.png');"></div>
        </div>

        <div class="trust-col trust-col-peak">
          <div class="trust-img trust-img-xl" style="background-image:url('/images/8.png');"></div>
        </div>

        <div class="trust-col trust-col-peak">
          <div class="trust-img trust-img-xl" style="background-image:url('/images/9.png');"></div>
        </div>

        <div class="trust-col trust-col-mid">
          <div class="trust-img trust-img-lg" style="background-image:url('/images/10.png');"></div>
        </div>

        <div class="trust-col trust-col-short">
          <div class="trust-img" style="background-image:url('/images/11.png');"></div>
          <div class="trust-img" style="background-image:url('/images/12.png');"></div>
          <div class="trust-img" style="background-image:url('/images/13.png');"></div>
        </div>

        <div class="trust-col trust-col-tall">
          <div class="trust-img" style="background-image:url('/images/14.png');"></div>
          <div class="trust-img" style="background-image:url('/images/15.png');"></div>
        </div>

      </div>

      <div class="trust-heading">
        <span class="trust-pill">Testimonials</span>
        <h2 class="trust-title">
          Trusted by creatives and leaders<br />
          <span class="trust-title-sub">from various industries</span>
        </h2>
        <p class="trust-blurb">
          For brands, events, and communities, we turn everyday gifting into
          something more thoughtful, more memorable, and more human.
        </p>
      </div>

      <div class="trust-cards">
        <div class="trust-card">
          <div class="trust-stars">★★★★★</div>
          <p class="trust-quote">"WorkNook makes finding a coworking space so easy! I can book a desk in minutes and get straight to work. Highly recommend!"</p>
          <div class="trust-author">
            <div class="trust-avatar" style="background-image:url('/images/A1.png');"></div>
            <div>
              <p class="trust-name">Joao M.</p>
              <p class="trust-role">Startup Founder</p>
            </div>
          </div>
        </div>

        <div class="trust-card">
          <div class="trust-stars">★★★★★</div>
          <p class="trust-quote">Our team needed a flexible meeting space, and WorkNook delivered. The process was smooth, and the space was exactly what we needed!</p>
          <div class="trust-author">
            <div class="trust-avatar" style="background-image:url('/images/A2.png');"></div>
            <div>
              <p class="trust-name">Bruno K.</p>
              <p class="trust-role">UX Designer</p>
            </div>
          </div>
        </div>

        <div class="trust-card">
          <div class="trust-stars">★★★★★</div>
          <p class="trust-quote">"I love the variety of spaces available! Whether I need a quiet spot or a collaborative space, WorkNook always has the perfect option."</p>
          <div class="trust-author">
            <div class="trust-avatar" style="background-image:url('/images/A3.png');"></div>
            <div>
              <p class="trust-name">Lais A.</p>
              <p class="trust-role">Digital Marketer</p>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Product Catalog -->
    <section class="catalog-section" id="catalog">
      <div class="catalog-inner">

        <!-- Sidebar Filters -->
        <aside class="catalog-sidebar">
          <p class="sidebar-label">Categories</p>
          <ul class="filter-list">
            {categories.map(cat => (
              <li>
                <button
                  class="filter-btn category-btn"
                  data-category={cat.id}
                  data-active={cat.id === 'all' ? 'true' : 'false'}
                >
                  {cat.label}
                </button>
              </li>
            ))}
          </ul>

          {taxonomy.series.length > 0 && (
            <>
              <p class="sidebar-label sidebar-label-spaced">Series</p>
              <ul class="filter-list checkbox-filter-list">
                {taxonomy.series.map(s => (
                  <li>
                    <label class="checkbox-filter">
                      <input type="checkbox" class="series-filter" value={s} />
                      <span>{s}</span>
                    </label>
                  </li>
                ))}
              </ul>
            </>
          )}

          {taxonomy.giftLevels.length > 0 && (
            <>
              <p class="sidebar-label sidebar-label-spaced">Gift Levels</p>
              <ul class="filter-list checkbox-filter-list">
                {taxonomy.giftLevels.map(g => (
                  <li>
                    <label class="checkbox-filter">
                      <input type="checkbox" class="giftlevel-filter" value={g} />
                      <span>{g}</span>
                    </label>
                  </li>
                ))}
              </ul>
            </>
          )}

          {taxonomy.tags.length > 0 && (
            <>
              <p class="sidebar-label sidebar-label-spaced">Tags</p>
              <ul class="filter-list checkbox-filter-list">
                {taxonomy.tags.map(t => (
                  <li>
                    <label class="checkbox-filter">
                      <input type="checkbox" class="tag-filter" value={t} />
                      <span>{t}</span>
                    </label>
                  </li>
                ))}
              </ul>
            </>
          )}
        </aside>

        <!-- Product Grid -->
        <div class="product-grid" id="productGrid">
          {products.map(product => (
            <div
              class="product-card"
              data-category={product.category}
              data-series={(product.series || []).join('|')}
              data-giftlevels={(product.giftLevels || []).join('|')}
              data-tags={(product.tags || []).join('|')}
            >
              <div class="product-img-wrap">
                <img src={product.img} alt={product.name} class="product-img" />
              </div>
              <div class="product-info">
                <h3 class="product-name">{product.name}</h3>
                <p class="product-desc">{product.desc}</p>
                <a href="/get-in-touch" class="product-cta">Enquire</a>
              </div>
            </div>
          ))}
        </div>

      </div>
    </section>

  </main>

  <Footer />
</Layout>

<style is:global>
  .product-page {
    padding-top: 72px;
  }

  /* ─── CATALOG ─── */
  .catalog-section {
    padding: 4rem clamp(2rem, 6vw, 6rem);
  }

  .catalog-inner {
    display: grid;
    grid-template-columns: 220px 1fr;
    gap: 4rem;
    align-items: start;
  }

  /* ─── SIDEBAR ─── */
  .catalog-sidebar {
    position: sticky;
    top: 100px;
    max-height: calc(100vh - 130px);
    overflow-y: auto;
  }

  .sidebar-label {
    font-family: 'DM Sans', sans-serif;
    font-size: 0.8rem;
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: #000000;
    opacity: 0.4;
    margin: 0 0 1rem 0;
  }

  .sidebar-label-spaced {
    margin-top: 1.75rem;
  }

  .filter-list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .filter-btn {
    width: 100%;
    text-align: left;
    font-family: 'DM Sans', sans-serif;
    font-size: 0.95rem;
    font-weight: 400;
    color: #000000;
    background: transparent;
    border: 1px solid rgba(0,0,0,0.15);
    border-radius: 0;
    padding: 0.5rem 1rem;
    cursor: pointer;
    transition: background 0.2s ease, color 0.2s ease, border-color 0.2s ease;
  }

  .filter-btn:hover {
    background: rgba(149, 189, 141, 0.1);
    border-color: #95BD8D;
  }

  .filter-btn[data-active="true"] {
    background: #95BD8D;
    color: #ffffff;
    border-color: #95BD8D;
    font-weight: 600;
  }

  .checkbox-filter-list {
    gap: 0.4rem;
  }

  .checkbox-filter {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-family: 'DM Sans', sans-serif;
    font-size: 0.88rem;
    color: #000000;
    opacity: 0.75;
    cursor: pointer;
  }

  .checkbox-filter input {
    accent-color: #95BD8D;
    width: 15px;
    height: 15px;
    cursor: pointer;
  }

  /* ─── PRODUCT GRID ─── */
  .product-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
  }

  .product-card {
    display: flex;
    flex-direction: column;
    border: 1px solid rgba(0,0,0,0.08);
    transition: box-shadow 0.2s ease, transform 0.2s ease;
  }

  .product-card:hover {
    box-shadow: 0 8px 24px rgba(0,0,0,0.08);
    transform: translateY(-4px);
  }

  .product-card.hidden {
    display: none;
  }

  .product-img-wrap {
    width: 100%;
    aspect-ratio: 4/3;
    overflow: hidden;
    background: #f0f4f0;
  }

  .product-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.3s ease;
  }

  .product-card:hover .product-img {
    transform: scale(1.03);
  }

  .product-info {
    padding: 1.25rem;
    display: flex;
    flex-direction: column;
    flex: 1;
  }

  .product-name {
    font-family: 'DM Sans', sans-serif;
    font-weight: 700;
    font-size: 1rem;
    color: #000000;
    margin: 0 0 0.5rem 0;
  }

  .product-desc {
    font-family: 'DM Sans', sans-serif;
    font-size: 0.875rem;
    color: #000000;
    opacity: 0.6;
    margin: 0 0 1.25rem 0;
    line-height: 1.6;
    flex: 1;
  }

  .product-cta {
    display: inline-block;
    font-family: 'DM Sans', sans-serif;
    font-size: 0.875rem;
    font-weight: 600;
    color: #ffffff;
    background: #95BD8D;
    text-decoration: none;
    padding: 0.5rem 1.25rem;
    border-radius: 0;
    align-self: flex-start;
    transition: background 0.2s ease;
  }

  .product-cta:hover {
    background: #C08EA4;
  }

  /* ─── MOBILE ─── */
  @media (max-width: 768px) {
    .catalog-inner {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .catalog-sidebar {
      position: static;
      max-height: none;
    }

    .filter-list {
      flex-direction: row;
      flex-wrap: wrap;
    }

    .filter-btn {
      width: auto;
    }

    .checkbox-filter-list {
      flex-direction: row;
      flex-wrap: wrap;
      gap: 0.75rem 1.25rem;
    }

    .product-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 480px) {
    .product-grid {
      grid-template-columns: 1fr;
    }
  }
</style>

<style>
  .cycle-showcase {
    position: relative;
    background: #F7F6F3;
    padding: 6rem 1.5rem 5rem;
    overflow: hidden;
  }
 
  /* ─── Hero row: letter + circles + letter ─── */
  .cycle-hero-row {
    position: relative;
    max-width: 1300px;
    margin: 0 auto;
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: clamp(20rem, 36vw, 32rem);
  }
 
  .bg-letter {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    z-index: 0;
    font-family: 'MaoWei', serif;
    font-size: clamp(14rem, 26vw, 28rem);
    font-weight: 800;
    line-height: 1;
    color: transparent;
    -webkit-text-stroke: 1.5px #95BD8D;
    pointer-events: none;
  }
 
  .bg-letter-left { left: clamp(-7rem, -9vw, -3rem); }
  .bg-letter-right { right: clamp(-5rem, -7vw, -2rem); }
 
  /* ─── Circles (Venn layout) ─── */
  .cycle-circles {
    position: relative;
    z-index: 5;
    display: flex;
    justify-content: center;
    align-items: center;
    isolation: isolate;
  }
 
  .cycle-circle {
    flex: 0 0 auto;
    width: min(30vw, 380px);
    aspect-ratio: 1;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
    box-shadow: 0 0 0 8px #F7F6F3;
  }
 
  .circle-message {
    background: #95BD8D;
    color: #ffffff;
    flex-direction: column;
    align-items: flex-start;
    justify-content: center;
    padding: 3rem;
    text-align: left;
    margin-right: -8%;
    z-index: 10;
  }
 
  .circle-arrow {
    font-size: 1.6rem;
    margin-bottom: 0.1rem;
  }
 
  .circle-message p {
    font-family: 'MaoWei', sans-serif;
    font-size: 16px;
    font-weight: 500;
    line-height: 1.4;
    margin: 0;
    max-width: 21ch;
  }
 
  .circle-photo {
    background-size: cover;
    background-position: center;
    background-color: #444;
  }
 
  .circle-photo-1 {
    z-index: 20;
  }
 
  .circle-photo-2 {
    margin-left: -6%;
    z-index: 30;
  }
 
  .cycle-wordmark {
    position: absolute;
    top: 50%;
    left: 52%;
    transform: translate(-50%, -50%);
    z-index: 40;
    font-family: 'MaoWei', serif;
    font-weight: 800;
    font-size: clamp(2.8rem, 5.8vw, 4.8rem);
    color: #ffffff;
    letter-spacing: 0.02em;
    text-align: center;
    /* text-shadow: 0 2px 20px rgba(0, 0, 0, 0.55); */
    padding: 0 1rem;
    white-space: nowrap;
    pointer-events: none;
  }
 
  .cycle-learn-more {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    background: #ffffff;
    color: #1f2e1c;
    font-family: 'MaoWei', sans-serif;
    font-weight: 600;
    font-size: 0.9rem;
    padding: 0.65rem 1.2rem;
    border-radius: 999px;
    text-decoration: none;
    white-space: nowrap;
    transition: transform 0.2s ease;
  }
  .cycle-learn-more:hover {
    transform: translateY(-2px);
  }
 
  /* ─── Cards row ─── */
  .cycle-cards {
    position: relative;
    z-index: 1;
    max-width: 1200px;
    margin: 4.5rem auto 0;
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.5rem;
  }
 
  .cycle-card {
    position: relative;
    display: flex;
    align-items: stretch;
    aspect-ratio: 2.2 / 1;
    background: #232320;
    border-radius: 20px;
    overflow: hidden;
  }

  /* Left panel — text, 50% */
  .cycle-card-text {
    position: relative;
    z-index: 3;
    flex: 0 0 50%;
    width: 50%;
    padding: clamp(1.25rem, 3.5vw, 2.25rem);
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: flex-start;
    gap: clamp(1rem, 2.5vw, 1.5rem);
  }

  .cycle-card-text h3 {
    font-family: 'MaoWei', sans-serif;
    font-weight: 700;
    font-size: clamp(1.05rem, 2vw, 1.35rem);
    line-height: 1.25;
    color: #ffffff;
    margin: 0;
  }

  /* Right panel — photo, 50% */
  .cycle-card-photo {
    position: relative;
    z-index: 1;
    flex: 0 0 50%;
    width: 50%;
    background-size: cover;
    background-position: center;
  }

  /* The "bite" circle — straddles the seam, scales with card width via % */
  .cycle-card-circle {
    position: absolute;
    z-index: 2;
    top: 50%;
    left: 50%;
    width: 38%;
    aspect-ratio: 1;
    transform: translate(-50%, -50%);
    border-radius: 50%;
    overflow: hidden;
    background-size: cover;
    background-position: center;
    pointer-events: none;
    transition: transform 0.6s ease;
  }

  .cycle-card:hover .cycle-card-circle {
    transform: translate(-50%, -50%) rotate(360deg) scale(1.08);
  }

  .cycle-card-circle::after {
    content: '';
    position: absolute;
    top: 0;
    right: 0;
    width: 50%;
    height: 100%;
    background: #232320;
  }

  .cycle-card-arrow {
    position: absolute;
    z-index: 4;
    top: 1rem;
    right: 1rem;
    width: 34px;
    height: 34px;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.15);
    color: #ffffff;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1rem;
  }
 
  .cycle-explore-btn {
    align-self: flex-start;
    font-family: 'MaoWei', sans-serif;
    font-size: 0.85rem;
    font-weight: 600;
    color: #1f2e1c;
    background: #95BD8D;
    text-decoration: none;
    padding: 0.5rem 1.25rem;
    border-radius: 999px;
    transition: background 0.2s ease;
  }
  .cycle-explore-btn:hover {
    background: #7fa878;
  }
 
  /* ─── Mobile ─── */
  @media (max-width: 900px) {
    .cycle-hero-row {
      min-height: 0;
    }
    .cycle-circles {
      flex-direction: column;
    }
    .cycle-circle {
      width: min(70vw, 320px);
      margin: 0 0 -12% !important;
    }
    .circle-message {
      align-items: center;
      text-align: center;
      padding: 2.5rem 2rem;
    }
    .circle-message p {
      max-width: none;
    }
    .bg-letter {
      display: none;
    }
    .cycle-cards {
      grid-template-columns: 1fr;
      margin-top: 3.5rem;
    }

    .cycle-card {
      flex-direction: column;
      aspect-ratio: auto;
    }

    .cycle-card-text,
    .cycle-card-photo {
      width: 100%;
      flex: none;
    }

    .cycle-card-text {
      padding: 1.75rem 1.75rem 2.5rem;
    }

    .cycle-card-photo {
      aspect-ratio: 16 / 10;
    }

    .cycle-card-circle {
      top: auto;
      bottom: 0;
      left: 50%;
      width: 55%;
    }
  }
</style>

<!-- ─── HERO STYLES + SCRIPT ─── -->
<style>
  .hero-gifts {
    position: relative;
    width: 100%;
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
    background: #faf8f4;
  }

  #giftCanvas {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    display: block;
  }

  .hero-content {
    position: relative;
    z-index: 2;
    text-align: center;
    max-width: 90vw;
    margin: 0 auto;
    padding: 0 1.5rem;
    pointer-events: none;
  }

  .hero-content h1 {
    font-family: 'MaoWei', serif;
    font-weight: 800;
    line-height: 1.15;
    margin: 0 auto 1.25rem;
    width: 100%;
    text-align: center;
  }

  .line-big {
    display: block;
    width: 100%;
    white-space: nowrap;
    font-size: clamp(1.75rem, 5.2vw, 4.5rem);
    color: #2b2b28;
    text-align: center;
  }

  .line-small {
    font-family: 'MaoWei', serif;
    font-size: clamp(1.1rem, 2.2vw, 1.5rem);
    color: #5f5e5a;
    margin: 0 0 2.25rem;
    line-height: 1.5;
  }

  .hero-actions {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
    pointer-events: auto;
  }

  .btn {
    display: inline-block;
    padding: 0.85rem 1.75rem;
    border-radius: 999px;
    font-size: 1rem;
    font-weight: 500;
    text-decoration: none;
    transition: transform 0.2s ease, background 0.2s ease, color 0.2s ease;
  }

  .btn-ghost {
    border: 1.5px solid #95bd8d;
    color: #3b6d11;
    background: transparent;
  }
  .btn-ghost:hover {
    background: #95bd8d;
    color: #fff;
    transform: translateY(-2px);
  }

  .btn-solid {
    background: #95bd8d;
    color: #fff;
    border: 1.5px solid #95bd8d;
  }
  .btn-solid:hover {
    background: #7fa878;
    transform: translateY(-2px);
  }

  @media (max-width: 640px) {
    .hero-gifts {
      min-height: 100svh;
    }
    .hero-actions {
      flex-direction: column;
      align-items: center;
      width: 100%;
    }
    .btn {
      width: 100%;
      max-width: 260px;
      text-align: center;
    }
    .line-big {
      white-space: normal;
      font-size: clamp(1.5rem, 6.5vw, 2.1rem);
      text-align: center;
    }
    .hero-content {
      max-width: 100%;
      padding: 0 1rem;
    }
  }
</style>

<style>
  .trust-section {
    position: relative;
    width: 100%;
    background: #ffffff;
    padding: 5rem 1.5rem 6rem;
    overflow: hidden;
  }

  /* ─── Collage ─── */
  .trust-collage {
    max-width: 1300px;
    margin: 0 auto -9rem;
    display: flex;
    justify-content: center;
    align-items: flex-end;
    gap: clamp(0.6rem, 1.4vw, 1.1rem);
  }

  .trust-col {
    display: flex;
    flex-direction: column;
    gap: clamp(0.6rem, 1.4vw, 1.1rem);
    flex: 1 1 0;
    min-width: 0;
  }

  /* Arc heights: outer columns lower, inner columns taller */
  .trust-col-tall   { align-self: flex-end; }
  .trust-col-short  { align-self: flex-end; padding-bottom: clamp(1rem, 3vw, 2.5rem); }
  .trust-col-mid    { align-self: flex-start; }
  .trust-col-peak   { align-self: flex-start; }

  .trust-img {
    width: 100%;
    aspect-ratio: 3 / 2.6;
    border-radius: 16px;
    background-size: cover;
    background-position: center;
    background-color: #eceae5; /* fallback while loading */
    position: relative;
  }

  .trust-img-lg { aspect-ratio: 3 / 3.6; }
  .trust-img-xl { aspect-ratio: 3 / 5.2; }

  /* ─── Heading block ─── */
  .trust-heading {
    position: relative;
    z-index: 2;
    max-width: 45rem;
    margin: 0 auto 4rem;
    text-align: center;
  }

  .trust-pill {
    display: inline-block;
    font-family: 'DM Sans', sans-serif;
    font-size: 0.85rem;
    font-weight: 500;
    color: #2b2b28;
    border: 1px solid rgba(0,0,0,0.15);
    border-radius: 999px;
    padding: 0.45rem 1.25rem;
    margin-bottom: 1.5rem;
  }

  .trust-title {
    font-family: 'MaoWei', serif;
    font-weight: 800;
    font-size: clamp(1.2rem, 3vw, 2.1rem);
    line-height: 1.25;
    color: #1f1f1c;
    margin: 0 0 1.25rem;
  }

  .trust-title-sub {
    color: #a9b3c4;
  }

  .trust-blurb {
    font-family: 'DM Sans', sans-serif;
    font-size: 1rem;
    line-height: 1.65;
    color: #6b6a65;
    margin: 0 auto;
    max-width: 34rem;
  }

  /* ─── Testimonial cards ─── */
  .trust-cards {
    max-width: 1200px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 3rem;
  }

  .trust-card {
    display: flex;
    flex-direction: column;
    gap: 0.85rem;
  }

  .trust-stars {
    color: #e8b23d;
    font-size: 1rem;
    letter-spacing: 0.15em;
  }

  .trust-quote {
    font-family: 'DM Sans', sans-serif;
    font-size: 0.95rem;
    line-height: 1.6;
    color: #4a4a45;
    margin: 0;
  }

  .trust-author {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    margin-top: 0.25rem;
  }

  .trust-avatar {
    width: 42px;
    height: 42px;
    border-radius: 50%;
    background-size: cover;
    background-position: center;
    background-color: #dedad2;
    flex-shrink: 0;
  }

  .trust-name {
    font-family: 'DM Sans', sans-serif;
    font-weight: 700;
    font-size: 0.9rem;
    color: #1f1f1c;
    margin: 0;
  }

  .trust-role {
    font-family: 'DM Sans', sans-serif;
    font-size: 0.82rem;
    color: #8a8983;
    margin: 0.1rem 0 0;
  }

  /* ─── Mobile ─── */
  @media (max-width: 900px) {
    .trust-collage {
      display: none; /* collage gets busy on small screens; hide and let heading lead */
    }

    .trust-heading {
      margin-bottom: 3rem;
    }

    .trust-cards {
      grid-template-columns: 1fr;
      gap: 2.5rem;
    }
  }
</style>

<script is:inline>
  (function () {
    function init() {
      var canvas = document.getElementById('giftCanvas');
      var section = canvas.closest('.hero-gifts');
      var ctx = canvas.getContext('2d');
      var W, H;

      function resize() {
        W = section.clientWidth;
        H = section.clientHeight;
        canvas.width = W * devicePixelRatio;
        canvas.height = H * devicePixelRatio;
        canvas.style.width = W + 'px';
        canvas.style.height = H + 'px';
        ctx.setTransform(devicePixelRatio, 0, 0, devicePixelRatio, 0, 0);
      }
      resize();
      window.addEventListener('resize', resize);

      var palette = [
        { box: '#95BD8D', ribbon: '#5F8A57' },
        { box: '#E8C1A0', ribbon: '#C68A55' },
        { box: '#D8A0A8', ribbon: '#B06B78' },
        { box: '#B7C7DA', ribbon: '#7E97B5' },
        { box: '#EFE3C8', ribbon: '#CBAE7A' }
      ];

      function drawGiftBox(ctx, size, colors, rotation) {
        var s = size;
        ctx.save();
        ctx.rotate(rotation);

        ctx.fillStyle = colors.box;
        ctx.fillRect(-s / 2, -s / 2, s, s);

        ctx.fillStyle = colors.ribbon;
        ctx.fillRect(-s * 0.09, -s / 2, s * 0.18, s);
        ctx.fillRect(-s / 2, -s * 0.09, s, s * 0.18);

        ctx.beginPath();
        ctx.ellipse(-s * 0.16, -s / 2, s * 0.14, s * 0.1, 0.5, 0, Math.PI * 2);
        ctx.ellipse(s * 0.16, -s / 2, s * 0.14, s * 0.1, -0.5, 0, Math.PI * 2);
        ctx.fill();
        ctx.beginPath();
        ctx.arc(0, -s / 2, s * 0.08, 0, Math.PI * 2);
        ctx.fill();

        ctx.restore();
      }

      var N = 26;
      var boxes = [];
      for (var i = 0; i < N; i++) {
        var ang = Math.random() * Math.PI * 2;
        var speed = 0.1 + Math.random() * 0.18;
        boxes.push({
          x: Math.random() * W,
          y: Math.random() * H,
          vx: Math.cos(ang) * speed,
          vy: Math.sin(ang) * speed,
          size: 80 + Math.pow(Math.random(), 1.3) * 160,
          rot: Math.random() * Math.PI,
          rotSpeed: (Math.random() - 0.5) * 0.004,
          colors: palette[i % palette.length],
          opacity: 0.55 + Math.random() * 0.4
        });
      }

      var mouse = { x: -9999, y: -9999, active: false };
      section.addEventListener('mousemove', function (e) {
        var rect = section.getBoundingClientRect();
        mouse.x = e.clientX - rect.left;
        mouse.y = e.clientY - rect.top;
        mouse.active = true;
      });
      section.addEventListener('mouseleave', function () {
        mouse.active = false;
      });
      section.addEventListener(
        'touchmove',
        function (e) {
          var rect = section.getBoundingClientRect();
          var t = e.touches[0];
          mouse.x = t.clientX - rect.left;
          mouse.y = t.clientY - rect.top;
          mouse.active = true;
        },
        { passive: true }
      );

      var sweepRadius = 190;
      var maxSpeed = 6;

      function tick() {
        ctx.clearRect(0, 0, W, H);
        for (var i = 0; i < boxes.length; i++) {
          var b = boxes[i];

          if (mouse.active) {
            var dx = b.x - mouse.x,
              dy = b.y - mouse.y;
            var dist = Math.sqrt(dx * dx + dy * dy) || 0.001;
            if (dist < sweepRadius) {
              var force = (1 - dist / sweepRadius) * 2.6;
              b.vx += (dx / dist) * force;
              b.vy += (dy / dist) * force;
            }
          }

          b.vx *= 0.985;
          b.vy *= 0.985;

          var sp = Math.sqrt(b.vx * b.vx + b.vy * b.vy);
          if (sp > maxSpeed) {
            b.vx = (b.vx / sp) * maxSpeed;
            b.vy = (b.vy / sp) * maxSpeed;
          }
          if (sp < 0.08) {
            b.vx += (Math.random() - 0.5) * 0.04;
            b.vy += (Math.random() - 0.5) * 0.04;
          }

          b.x += b.vx;
          b.y += b.vy;
          b.rot += b.rotSpeed + b.vx * 0.002;

          var half = b.size / 2;
          if (b.x - half < 0) {
            b.x = half;
            b.vx *= -0.6;
          }
          if (b.x + half > W) {
            b.x = W - half;
            b.vx *= -0.6;
          }
          if (b.y - half < 0) {
            b.y = half;
            b.vy *= -0.6;
          }
          if (b.y + half > H) {
            b.y = H - half;
            b.vy *= -0.6;
          }

          ctx.save();
          ctx.globalAlpha = b.opacity;
          ctx.translate(b.x, b.y);
          drawGiftBox(ctx, b.size, b.colors, b.rot);
          ctx.restore();
        }
        requestAnimationFrame(tick);
      }
      tick();
    }

    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', init);
    } else {
      init();
    }
  })();
</script>

<!-- ─── PRODUCT FILTER SCRIPT (Category + Series + Gift Levels + Tags, all AND-combined) ─── -->
<script is:inline>
  (function () {
    var categoryBtns = document.querySelectorAll('.category-btn');
    var seriesChecks = document.querySelectorAll('.series-filter');
    var giftlevelChecks = document.querySelectorAll('.giftlevel-filter');
    var tagChecks = document.querySelectorAll('.tag-filter');
    var productCards = document.querySelectorAll('.product-card');

    var activeCategory = 'all';

    function getCheckedValues(nodeList) {
      return Array.prototype.slice.call(nodeList)
        .filter(function (cb) { return cb.checked; })
        .map(function (cb) { return cb.value; });
    }

    function matches(card) {
      if (activeCategory !== 'all' && card.getAttribute('data-category') !== activeCategory) {
        return false;
      }

      var series = getCheckedValues(seriesChecks);
      if (series.length) {
        var cardSeries = (card.getAttribute('data-series') || '').split('|');
        var hasSeries = series.some(function (s) { return cardSeries.indexOf(s) !== -1; });
        if (!hasSeries) return false;
      }

      var levels = getCheckedValues(giftlevelChecks);
      if (levels.length) {
        var cardLevels = (card.getAttribute('data-giftlevels') || '').split('|');
        var hasLevel = levels.some(function (l) { return cardLevels.indexOf(l) !== -1; });
        if (!hasLevel) return false;
      }

      var tags = getCheckedValues(tagChecks);
      if (tags.length) {
        var cardTags = (card.getAttribute('data-tags') || '').split('|');
        var hasTag = tags.some(function (t) { return cardTags.indexOf(t) !== -1; });
        if (!hasTag) return false;
      }

      return true;
    }

    function applyFilters() {
      productCards.forEach(function (card) {
        card.classList.toggle('hidden', !matches(card));
      });
    }

    categoryBtns.forEach(function (btn) {
      btn.addEventListener('click', function () {
        categoryBtns.forEach(function (b) { b.setAttribute('data-active', 'false'); });
        btn.setAttribute('data-active', 'true');
        activeCategory = btn.getAttribute('data-category');
        applyFilters();
      });
    });

    [seriesChecks, giftlevelChecks, tagChecks].forEach(function (group) {
      group.forEach(function (cb) {
        cb.addEventListener('change', applyFilters);
      });
    });
  })();
</script>

<!-- ─── INTRO / TYPED-WORD STYLES + SCRIPT ─── -->
<style>
  .intro-section {
    position: relative;
    width: 100%;
    min-height: 100vh;
    background: #ffffff;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 4rem 1.5rem;
    overflow: hidden;
  }

  .intro-section::before {
    content: '';
    position: absolute;
    inset: 0;
    background: url('/images/gradient-bg-1.png') center center / cover no-repeat;
    opacity: 0.6;
    z-index: 0;
  }

  .intro-content {
    position: relative;
    z-index: 1;
    max-width: 50rem;
    text-align: center;
  }

  .intro-title {
    font-family: 'MaoWei', serif;
    font-weight: 700;
    font-size: clamp(2rem, 5vw, 3.5rem);
    color: #2b2b28;
    margin: 0 0 1rem;
  }

  .intro-tagline {
    font-family: 'MaoWei', serif;
    font-size: clamp(1.1rem, 2.2vw, 1.6rem);
    color: #5f5e5a;
    margin: 0 0 2rem;
  }

  .intro-line {
    font-family: 'MaoWei', serif;
    font-weight: 500;
    font-size: clamp(1.4rem, 3.2vw, 2.4rem);
    line-height: 1.4;
    color: #2b2b28;
    margin: 0;
  }

  .typed-wrap {
    display: inline-flex;
    align-items: baseline;
  }

  .typed-word {
    color: #95bd8d;
    font-weight: 700;
    white-space: pre;
  }

  .typed-cursor {
    color: #95bd8d;
    font-weight: 400;
    margin-left: 2px;
    animation: blink 0.9s step-end infinite;
  }

  @keyframes blink {
    0%, 100% { opacity: 1; }
    50% { opacity: 0; }
  }

  @media (max-width: 640px) {
    .intro-section {
      min-height: 70vh;
      padding: 3rem 1.5rem;
    }
  }
</style>

<script is:inline>
  (function () {
    function init() {
      var el = document.getElementById('typedWord');
      if (!el) return;

      var words = ['people.', 'planet.', 'purpose.'];
      var wordIndex = 0;
      var charIndex = 0;
      var typing = true;
      var typeSpeed = 90;
      var deleteSpeed = 55;
      var pauseAfterType = 1100;
      var pauseAfterDelete = 250;

      function tick() {
        var current = words[wordIndex];

        if (typing) {
          charIndex++;
          el.textContent = current.slice(0, charIndex);
          if (charIndex === current.length) {
            typing = false;
            setTimeout(tick, pauseAfterType);
            return;
          }
          setTimeout(tick, typeSpeed);
        } else {
          charIndex--;
          el.textContent = current.slice(0, charIndex);
          if (charIndex === 0) {
            typing = true;
            wordIndex = (wordIndex + 1) % words.length;
            setTimeout(tick, pauseAfterDelete);
            return;
          }
          setTimeout(tick, deleteSpeed);
        }
      }

      tick();
    }

    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', init);
    } else {
      init();
    }
  })();
</script>