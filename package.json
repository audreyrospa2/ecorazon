{
  "name": "ecorazon-website",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
  "dev": "astro dev",
  "build": "astro build",
  "preview": "astro preview",
  "deploy": "npm run build && gh-pages -d dist --branch=gh-pages --repo=https://github.com/audreyrospa/ecorazon-site.git -f"
},
  "dependencies": {
    "@astrojs/tailwind": "^6.0.2",
    "astro": "^5.14.0"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.19",
    "gh-pages": "^6.3.0",
    "tailwindcss": "^3.4.17"
  }
}
